PORT=
NODE_ENV=
ALLOWED_ORIGINS=
# Database (Cloud - Neon)
DATABASE_URL=

# Local Database Fallback (used if DATABASE_URL is empty)
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Redis (Cloud - Upstash)
REDIS_HOST=
REDIS_PORT=
UPSTASH_REDIS_REST_URL=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=
